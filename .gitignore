.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.hypothesis/
.idea/
dist/
build/

# Artifacts written by the web app's export endpoints and the visualizer,
# which render to a file beside the process rather than to a temp dir.
ontodag_export.owl
ontodag_query_export_*.owl
ontodag_vis
ontodag_vis.*

# setuptools build output — accidentally committed 2026-07-28 in 4ba635e and
# stale ever since (it never tracked later source changes).
build/
